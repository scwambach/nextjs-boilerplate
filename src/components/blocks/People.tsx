import { ComponentProps } from '@utils/types'

// TODO: Create People component

interface PeopleProps extends ComponentProps {}

export const People = (props: PeopleProps) => {
  return (
    <div className={`people${props.className ? ` ${props.className}` : ''}`}>
      <code>
        <pre
          style={{
            fontFamily: 'monospace',
            display: 'block',
            padding: '50px',
            color: '#88ffbf',
            backgroundColor: 'black',
            textAlign: 'left',
            whiteSpace: 'pre-wrap',
          }}
        >
          {JSON.stringify(props, null, '    ')}
        </pre>
      </code>
    </div>
  )
}
